#!/bin/bash

cd "$(dirname "${BASH_SOURCE[0]}")" \
    && . "../../utils.sh"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

print_in_purple "\n   Bluetooth activation\n\n"

execute "sudo systemctl enable --now bluetooth.service" \
    "Bluez"
