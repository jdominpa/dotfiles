# -*- mode: snippet -*-
# name: malloc
# key: mal
# --
${1:vec} = (${2:double} *) malloc(${3:n} * sizeof($2));
if ($1 == NULL) {
    fprintf(stderr, "Memory allocation failed\n");
    ${4:exit(1);}
}$0